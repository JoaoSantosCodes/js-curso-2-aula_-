// Crie uma lista vazia chamada listaGenerica.
let listaGenerica = [];

// Crie uma lista de linguagens de programação chamada linguagensDeProgramacao.
let linguagensDeProgramacao = ['JavaScript', 'C', 'C++', 'Kotlin', 'Python'];

// Adicione à lista linguagensDeProgramacao os seguintes elementos: 'Java', 'Ruby' e 'GoLang'.
linguagensDeProgramacao.push('Java', 'Ruby', 'GoLang');

// Crie uma lista com 3 nomes.
let nomes = ['Alice', 'Bob', 'Charlie'];

// Exiba no console apenas o primeiro elemento da lista de nomes.
console.log('Primeiro elemento da lista de nomes:', nomes[0]);

// Exiba no console apenas o segundo elemento da lista de nomes.
console.log('Segundo elemento da lista de nomes:', nomes[1]);

// Exiba no console apenas o último elemento da lista de nomes.
console.log('Último elemento da lista de nomes:', nomes[nomes.length - 1]);
